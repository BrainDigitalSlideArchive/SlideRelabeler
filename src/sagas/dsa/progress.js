import {put, select, call, fork} from 'redux-saga/effects';

function *watch_progress(progress) {
}

// ipcRenderer.on('dsa-file-progress', (event, progress) => {
//     watch_progress();
// })

export default watch_progress;