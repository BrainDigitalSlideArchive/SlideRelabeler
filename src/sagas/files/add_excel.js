import { put, call, select } from 'redux-saga/effects';

import * as files_actions from "../../actions/files";

// todo: finish add excel case

export default function* add_excel(file) {
  console.log("Add execl saga...\n", file);
}